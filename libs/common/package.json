{
  "name": "@sandbox/common",
  "version": "1.0.0",
  "license": "MIT",
  "main": "dist/index",
  "types": "dist/index",
  "dependencies": {
    "@sandbox/string-utils": "1.0.0"
  },
  "scripts": {
    "clean": "rimraf dist *.tsbuildinfo",
    "typecheck": "yarn tsc --noEmit",
    "test": "yarn jest",
    "build": "yarn tsc --build"
  }
}
