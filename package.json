{
  "name": "typescript-monorepo",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "2.1.2",
    "rimraf": "3.0.2",
    "typescript": "4.0.5"
  },
  "scripts": {
    "clean": "yarn workspaces run clean",
    "typecheck": "yarn workspaces run typecheck",
    "build": "yarn workspace @sandbox/string-utils build && yarn workspace @sandbox/common build && yarn workspace @sandbox/app build",
    "dev": "yarn workspace @sandbox/app dev",
    "start": "yarn workspace @sandbox/app start"
  }
}
