{
  "name": "@sandbox/string-utils",
  "version": "1.0.0",
  "license": "MIT",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "clean": "rimraf dist tsconfig.tsbuildinfo",
    "typecheck": "yarn tsc --noEmit",
    "build": "yarn tsc --build"
  }
}
